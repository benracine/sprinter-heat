# Book build

One document. Two formats. No other reports.

```sh
./book/build.sh
```

Writes only:

- `out/heat-and-hot-water.html`
- `out/heat-and-hot-water.pdf`

**Part one:** `00`–`05`, `install-in-this-order.md`  
**Part two:** `front-matter.md`, `appendix/`, `research/`  
**Diagrams:** edit `diagrams/*.mmd`, keep matching `.svg` in sync  

Citations: `../sources/`. Photos: `../photos/` (paths only in prose).
